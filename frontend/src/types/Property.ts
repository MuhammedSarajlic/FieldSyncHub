export type TProperty = {
  id: string;
  street: string;
  city: string;
  state: string;
  country: string;
  address: string;
  postalCode: string;
  isBillingAddress: boolean;
  customerId: string;
};

export type TAddProperty = {
  street: string;
  city: string;
  state: string;
  country: string;
  postalCode: string;
  isBillingAddress: boolean;
  customerId: string;
};
