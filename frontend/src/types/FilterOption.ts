export type TFilterOption = {
  name: string;
  label: string;
  type: string;
  valueType?: string;
  dropdownOptions?: string[];
  placeholder?: string;
  options?: string[];
  min?: string;
  max?: string;
};
