import React, { useEffect, useState } from 'react';
import { AlertCircle, Building2, Check, ChevronDown, CircleDollarSign, FilePenLine, LoaderCircle, Mail, MapPin, Percent, Phone, Plus, Search, Trash2, User, UserRound, X } from 'lucide-react';
import { DiscountType } from '../../../constants/Enumeration/CommonEnum/DiscountEnum';
import { useAuth } from '../../../context/AuthProvider';
import { useClickOutside } from '../../../hooks/useClickOutside';
import { useDebounce } from '../../../hooks/useDebounce';
import { GetEmployeesByWorkspace } from '../../../services/Employee';
import { UpdateQuote } from '../../../services/Quote';
import { GetServiceItemsByFilter } from '../../../services/ServiceItem';
import { TEmployee } from '../../../types/Employee';
import { TAddLineItem, TUpdateLineItem } from '../../../types/LineItem';
import { TQuote, TUpdateQuote } from '../../../types/Quote';
import { TServiceItem } from '../../../types/ServiceItem';
import { formatCurrency } from '../../../utils/FuntionHelpers/formatCurrency';
import Modal from '../../CustomElements/Modal';

interface IEditQuoteModal { isOpen?: boolean; onClose: () => void; setQuotes: React.Dispatch<React.SetStateAction<TQuote | null>>; quoteToEdit: TQuote; }
const fieldClass = 'h-10 w-full rounded-md border border-[#cbd5d0] bg-white px-3 text-sm text-[#17211d] outline-none placeholder:text-[#829088] focus:border-[#0d5944] focus:ring-2 focus:ring-[#0d5944]/15 disabled:cursor-not-allowed disabled:bg-[#f1f4f2] disabled:text-[#829088]';
const textareaClass = 'w-full rounded-md border border-[#cbd5d0] bg-white px-3 py-2.5 text-sm text-[#17211d] outline-none placeholder:text-[#829088] focus:border-[#0d5944] focus:ring-2 focus:ring-[#0d5944]/15';

const EditQuoteModal = ({ isOpen = false, onClose, setQuotes, quoteToEdit }: IEditQuoteModal) => {
  const { user } = useAuth();
  const [quote, setQuote] = useState<TUpdateQuote>(() => makeQuote(quoteToEdit));
  const [employees, setEmployees] = useState<TEmployee[]>([]);
  const [filteredEmployees, setFilteredEmployees] = useState<TEmployee[]>([]);
  const [serviceResults, setServiceResults] = useState<TServiceItem[]>([]);
  const [employeeSearch, setEmployeeSearch] = useState('');
  const [serviceSearch, setServiceSearch] = useState('');
  const [activeLineIndex, setActiveLineIndex] = useState<number | null>(null);
  const [isAssigneeOpen, setIsAssigneeOpen] = useState(false);
  const [employeesLoading, setEmployeesLoading] = useState(false);
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState('');
  const [assigneeName, setAssigneeName] = useState(quoteToEdit.assignedToUser?.fullName ?? '');
  const debouncedEmployeeSearch = useDebounce(employeeSearch, 250);
  const debouncedServiceSearch = useDebounce(serviceSearch, 250);
  const assigneeRef = useClickOutside<HTMLDivElement>(() => setIsAssigneeOpen(false));
  const customer = quoteToEdit.customer;
  const property = customer?.properties?.find((item) => item.id === quote.propertyId);
  const totals = calculateTotals(quote);

  useEffect(() => { if (!isOpen) return; setQuote(makeQuote(quoteToEdit)); setAssigneeName(quoteToEdit.assignedToUser?.fullName ?? ''); setSubmitError(''); }, [isOpen, quoteToEdit]);
  useEffect(() => { const search = debouncedEmployeeSearch.trim().toLowerCase(); setFilteredEmployees(search ? employees.filter((employee) => employee.user.fullName.toLowerCase().includes(search) || employee.user.email.toLowerCase().includes(search)) : employees); }, [debouncedEmployeeSearch, employees]);
  useEffect(() => { if (!debouncedServiceSearch.trim() || activeLineIndex === null || !user?.workspace) { setServiceResults([]); return; } let active = true; GetServiceItemsByFilter(user.workspace.id, 1, 8, `q=${encodeURIComponent(debouncedServiceSearch.trim())}`).then((response) => { if (active && response.status === 200) setServiceResults(response.data.payload.items.slice(0, 6)); }).catch(() => { if (active) setServiceResults([]); }); return () => { active = false; }; }, [activeLineIndex, debouncedServiceSearch, user?.workspace?.id]);

  const loadEmployees = async () => { if (!user?.workspace || employees.length) return; setEmployeesLoading(true); try { const response = await GetEmployeesByWorkspace(user.workspace.id); if (response.status === 200) { setEmployees(response.data.payload); setFilteredEmployees(response.data.payload); } } finally { setEmployeesLoading(false); } };
  const updateField = (name: keyof TUpdateQuote, value: string | number) => { setQuote((current) => ({ ...current, [name]: value })); setSubmitError(''); };
  const updateLineItem = (index: number, field: keyof TAddLineItem, value: string | number | boolean) => { setQuote((current) => { const lineItems = [...current.lineItems]; const existing = lineItems[index]; const clearLink = existing.serviceItemId && ((field === 'name' && value !== existing.name) || (field === 'unitPrice' && value !== existing.unitPrice)); lineItems[index] = { ...existing, [field]: value, ...(clearLink ? { serviceItemId: undefined } : {}) } as TUpdateLineItem; return { ...current, lineItems }; }); setSubmitError(''); };
  const chooseService = (index: number, service: TServiceItem) => { setQuote((current) => { const lineItems = [...current.lineItems]; lineItems[index] = { ...lineItems[index], serviceItemId: service.id, name: service.name, description: service.description, unitPrice: service.unitPrice, isTaxable: service.isTaxable }; return { ...current, lineItems }; }); setServiceSearch(''); setServiceResults([]); setActiveLineIndex(null); };
  const removeLine = (index: number) => setQuote((current) => ({ ...current, lineItems: current.lineItems.filter((_, itemIndex) => itemIndex !== index) }));
  const handleClose = () => { setIsAssigneeOpen(false); setActiveLineIndex(null); setSubmitError(''); onClose(); };
  const handleSubmit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!user?.workspace) { setSubmitError('Your workspace is not available. Refresh the page and try again.'); return; } if (!quote.title.trim()) { setSubmitError('Add a clear quote title.'); document.getElementById('edit-quote-title')?.focus(); return; } const invalidLine = quote.lineItems.findIndex((item) => !item.name.trim() || item.quantity <= 0); if (invalidLine >= 0) { setSubmitError(`Complete the name and quantity for line item ${invalidLine + 1}.`); document.getElementById(`edit-quote-item-${invalidLine}`)?.focus(); return; } setIsSubmitting(true); setSubmitError(''); const payload: TUpdateQuote = { ...quote, taxRate: quote.taxRate / 100, lineItems: quote.lineItems.map((item) => ({ id: item.id, quantity: item.quantity, name: item.name, unitPrice: item.unitPrice, description: item.description, serviceItemId: item.serviceItemId, isTaxable: item.isTaxable })) }; try { const response = await UpdateQuote(payload); if (response.status === 200) { setQuotes((current) => current ? { ...current, ...response.data } : response.data); handleClose(); } else setSubmitError(response.data?.message ?? 'The quote could not be updated. Try again.'); } catch (error: unknown) { const message = typeof error === 'object' && error && 'response' in error ? (error as { response?: { data?: { message?: string } } }).response?.data?.message : undefined; setSubmitError(message ?? 'The quote could not be updated. Check your connection and try again.'); } finally { setIsSubmitting(false); } };

  if (!isOpen) return null;
  return <Modal open={isOpen} onClose={handleClose} title='Edit quote' labelledBy='edit-quote-dialog-label' className='flex h-[min(900px,calc(100vh-2rem))] max-w-[1180px] flex-col !overflow-hidden'>
    <header className='flex shrink-0 items-start justify-between border-b border-[#d9e0dc] bg-white px-5 py-4 sm:px-7'><div className='flex min-w-0 items-start gap-3'><span className='mt-0.5 flex h-9 w-9 shrink-0 items-center justify-center rounded-md bg-[#e8f1ed] text-[#0d5944]'><FilePenLine className='h-5 w-5' /></span><div><h2 id='edit-quote-dialog-label' className='text-xl font-semibold text-[#17211d]'>Edit quote</h2><p className='mt-1 text-sm text-[#65736c]'>Update the estimate details before you send or revise it.</p></div></div><button type='button' onClick={handleClose} aria-label='Close edit quote' className='ml-4 rounded-md p-2 text-[#65736c] hover:bg-[#f0f3f1] hover:text-[#17211d] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#0d5944]'><X className='h-5 w-5' /></button></header>
    <form onSubmit={handleSubmit} className='flex min-h-0 flex-1 flex-col'><div className='min-h-0 flex-1 overflow-y-auto'><div className='grid min-h-full lg:grid-cols-[minmax(0,1fr)_340px]'><div className='min-w-0 px-5 py-6 sm:px-7 lg:pr-8'>
      <section aria-labelledby='edit-customer-heading'><SectionHeading id='edit-customer-heading' title='Customer and service address' description='The customer is fixed for this quote. You can update the property where the work will happen.' /><div className='mt-4 grid gap-4 sm:grid-cols-2'><DisplayField label='Customer'><div id='edit-quote-customer' className={`${fieldClass} flex items-center gap-2 bg-[#f1f4f2]`}><Building2 className='h-4 w-4 shrink-0 text-[#456157]' /><span className='truncate'>{getCustomerName(customer)}</span></div></DisplayField><Field label='Service address' htmlFor='edit-quote-property' required><div className='relative'><select id='edit-quote-property' value={quote.propertyId} onChange={(event) => updateField('propertyId', event.target.value)} className={`${fieldClass} appearance-none pr-9`}><option value=''>Select a property</option>{customer?.properties?.map((item) => <option key={item.id} value={item.id}>{item.address}</option>)}</select><ChevronDown className='pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[#6b7972]' /></div></Field></div>{customer && <div className='mt-4 flex flex-wrap gap-x-5 gap-y-2 border-y border-[#e0e6e2] bg-[#f8faf9] px-3 py-3 text-sm text-[#536159]'><span className='inline-flex items-center gap-2 font-semibold text-[#24332c]'><Building2 className='h-4 w-4 text-[#456157]' />{getCustomerName(customer)}</span>{customer.emails?.[0] && <a href={`mailto:${customer.emails[0]}`} className='inline-flex items-center gap-1.5 hover:text-[#0d5944] hover:underline'><Mail className='h-3.5 w-3.5' />{customer.emails[0]}</a>}{customer.customerPhones?.[0]?.phoneNumber && <a href={`tel:${customer.customerPhones[0].phoneNumber}`} className='inline-flex items-center gap-1.5 hover:text-[#0d5944] hover:underline'><Phone className='h-3.5 w-3.5' />{customer.customerPhones[0].phoneNumber}</a>}</div>}</section>
      <section aria-labelledby='edit-details-heading' className='mt-8 border-t border-[#dfe5e1] pt-7'><SectionHeading id='edit-details-heading' title='Quote details' description='Keep the title and ownership clear for the next person who follows up.' /><div className='mt-4 grid gap-4 sm:grid-cols-2'><Field label='Quote title' htmlFor='edit-quote-title' required><input id='edit-quote-title' value={quote.title} onChange={(event) => updateField('title', event.target.value)} className={fieldClass} /></Field><Field label='Lead source' htmlFor='edit-quote-source' hint='Optional'><input id='edit-quote-source' value={quote.source ?? ''} onChange={(event) => updateField('source', event.target.value)} placeholder='Referral, website, phone...' className={fieldClass} /></Field></div><div className='mt-4 max-w-sm' ref={assigneeRef}><span className='mb-1.5 block text-sm font-semibold text-[#33423a]'>Assigned to</span><div className='relative'><button type='button' onClick={() => { setIsAssigneeOpen((open) => !open); void loadEmployees(); }} aria-expanded={isAssigneeOpen} className='flex h-10 w-full items-center justify-between rounded-md border border-[#cbd5d0] bg-white px-3 text-left text-sm text-[#24332c] hover:bg-[#f8faf9] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#0d5944]/30'><span className='flex min-w-0 items-center gap-2'><UserRound className='h-4 w-4 shrink-0 text-[#456157]' /><span className='truncate'>{assigneeName || 'Unassigned'}</span></span><ChevronDown className='h-4 w-4 text-[#6b7972]' /></button>{isAssigneeOpen && <div className='absolute left-0 top-12 z-30 w-full min-w-[280px] overflow-hidden rounded-md border border-[#cbd5d0] bg-white shadow-[0_10px_28px_rgba(23,33,29,0.16)]'><label htmlFor='edit-quote-assignee-search' className='relative block border-b border-[#e0e6e2] p-2'><span className='sr-only'>Search team members</span><Search className='pointer-events-none absolute left-5 top-1/2 h-4 w-4 -translate-y-1/2 text-[#718078]' /><input id='edit-quote-assignee-search' value={employeeSearch} onChange={(event) => setEmployeeSearch(event.target.value)} placeholder='Search team members' className='h-9 w-full rounded-md bg-[#f4f7f5] pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-[#0d5944]/25' /></label><div className='max-h-56 overflow-y-auto py-1'><button type='button' onClick={() => { updateField('assignedToUserId', ''); setAssigneeName(''); setIsAssigneeOpen(false); }} className='flex w-full px-3 py-2 text-left text-sm text-[#536159] hover:bg-[#f2f6f3]'>Unassigned</button>{employeesLoading ? <div className='flex justify-center p-5'><LoaderCircle className='h-5 w-5 animate-spin text-[#0d5944]' /></div> : filteredEmployees.map((employee) => <button key={employee.id} type='button' onClick={() => { updateField('assignedToUserId', employee.user.id); setAssigneeName(employee.user.fullName); setIsAssigneeOpen(false); }} className='flex w-full items-center gap-3 px-3 py-2 text-left hover:bg-[#f2f6f3]'><span className='flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-[#e6eeea] text-xs font-bold text-[#24533f]'>{getInitials(employee.user.fullName)}</span><span className='min-w-0'><span className='block truncate text-sm font-semibold text-[#24332c]'>{employee.user.fullName}</span><span className='block truncate text-xs text-[#718078]'>{employee.user.email}</span></span></button>)}{!employeesLoading && filteredEmployees.length === 0 && <p className='px-3 py-5 text-center text-sm text-[#718078]'>No team members found.</p>}</div></div>}</div></div></section>
      <section aria-labelledby='edit-line-items-heading' className='mt-8 border-t border-[#dfe5e1] pt-7'><div className='flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between'><SectionHeading id='edit-line-items-heading' title='Line items' description='Search your pricebook or refine a custom service or material.' /><div className='flex gap-2'><button type='button' onClick={() => setQuote((current) => ({ ...current, lineItems: [...current.lineItems, makeLineItem()] }))} className='inline-flex h-9 items-center gap-1.5 rounded-md border border-[#cbd5d0] bg-white px-3 text-sm font-semibold text-[#34443c] hover:bg-[#f2f5f3]'><Plus className='h-4 w-4' /> Add item</button><button type='button' onClick={() => setQuote((current) => ({ ...current, lineItems: [...current.lineItems, makeLineItem(true)] }))} className='inline-flex h-9 items-center gap-1.5 rounded-md border border-[#cbd5d0] bg-white px-3 text-sm font-semibold text-[#34443c] hover:bg-[#f2f5f3]'><Plus className='h-4 w-4' /> Optional</button></div></div><div className='mt-4 divide-y divide-[#dfe5e1] border-y border-[#dfe5e1]'>{quote.lineItems.map((item, index) => <div key={`${item.id ?? 'new'}-${index}`} className='py-5'><div className='mb-3 flex items-center justify-between md:hidden'><span className='text-sm font-semibold text-[#33423a]'>Item {index + 1}</span><span className='font-semibold tabular-nums text-[#17211d]'>{formatCurrency(item.quantity * item.unitPrice)}</span></div><div className='grid gap-3 md:grid-cols-[minmax(220px,1fr)_90px_130px_120px_36px] md:items-start'><div className='min-w-0'><span id={`edit-quote-item-${index}-label`} className='mb-1.5 block text-sm font-semibold text-[#33423a]'>Item or service</span><div className='relative'><input id={`edit-quote-item-${index}`} aria-labelledby={`edit-quote-item-${index}-label`} value={item.name} onFocus={() => { setActiveLineIndex(index); setServiceSearch(item.name); }} onChange={(event) => { setActiveLineIndex(index); setServiceSearch(event.target.value); updateLineItem(index, 'name', event.target.value); }} onBlur={() => window.setTimeout(() => setActiveLineIndex(null), 180)} placeholder='Search pricebook or type a name' className={fieldClass} />{activeLineIndex === index && serviceResults.length > 0 && <div className='absolute left-0 top-11 z-30 max-h-64 w-full min-w-[300px] overflow-y-auto rounded-md border border-[#cbd5d0] bg-white py-1 shadow-[0_10px_28px_rgba(23,33,29,0.16)]'>{serviceResults.map((service) => <button key={service.id} type='button' onMouseDown={(event) => event.preventDefault()} onClick={() => chooseService(index, service)} className='flex w-full items-start justify-between gap-4 px-3 py-2.5 text-left hover:bg-[#f2f6f3]'><span className='min-w-0'><span className='block truncate text-sm font-semibold text-[#24332c]'>{service.name}</span><span className='mt-0.5 line-clamp-1 text-xs text-[#718078]'>{service.description || service.category}</span></span><span className='shrink-0 text-sm font-semibold tabular-nums text-[#0d5944]'>{formatCurrency(service.unitPrice)}</span></button>)}</div>}</div></div><Field label='Quantity' htmlFor={`edit-quote-quantity-${index}`}><input id={`edit-quote-quantity-${index}`} type='number' min='0.001' step='0.001' value={item.quantity} onChange={(event) => updateLineItem(index, 'quantity', Number(event.target.value) || 0)} className={`${fieldClass} tabular-nums`} /></Field><Field label='Unit price' htmlFor={`edit-quote-price-${index}`}><div className='relative'><CircleDollarSign className='pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[#718078]' /><input id={`edit-quote-price-${index}`} type='number' min='0' step='0.01' value={item.unitPrice} onChange={(event) => updateLineItem(index, 'unitPrice', Number(event.target.value) || 0)} className={`${fieldClass} pl-9 tabular-nums`} /></div></Field><div className='hidden pt-7 text-right md:block'><span className='font-semibold tabular-nums text-[#17211d]'>{formatCurrency(item.quantity * item.unitPrice)}</span></div><button type='button' onClick={() => removeLine(index)} disabled={quote.lineItems.length === 1} aria-label={`Remove line item ${index + 1}`} className='mt-7 hidden rounded-md p-2 text-[#718078] hover:bg-red-50 hover:text-red-700 disabled:cursor-not-allowed disabled:opacity-30 md:inline-flex'><Trash2 className='h-4 w-4' /></button></div><div className='mt-3 grid gap-3 md:grid-cols-[minmax(0,1fr)_auto] md:items-start'><Field label='Description' htmlFor={`edit-quote-description-${index}`} hint='Optional'><textarea id={`edit-quote-description-${index}`} rows={2} value={item.description ?? ''} onChange={(event) => updateLineItem(index, 'description', event.target.value)} placeholder='Scope, materials, exclusions, or other details' className={`${textareaClass} resize-y`} /></Field><div className='flex flex-wrap items-center gap-4 pt-1 md:pt-7'><label htmlFor={`edit-quote-taxable-${index}`} className='inline-flex cursor-pointer items-center gap-2 text-sm font-medium text-[#536159]'><input id={`edit-quote-taxable-${index}`} type='checkbox' checked={Boolean(item.isTaxable)} onChange={(event) => updateLineItem(index, 'isTaxable', event.target.checked)} className='h-4 w-4 rounded border-[#aebbb4] text-[#0d5944] focus:ring-[#0d5944]' />Taxable</label><label htmlFor={`edit-quote-optional-${index}`} className='inline-flex cursor-pointer items-center gap-2 text-sm font-medium text-[#536159]'><input id={`edit-quote-optional-${index}`} type='checkbox' checked={Boolean(item.isOptional)} onChange={(event) => updateLineItem(index, 'isOptional', event.target.checked)} className='h-4 w-4 rounded border-[#aebbb4] text-[#0d5944] focus:ring-[#0d5944]' />Optional</label><button type='button' onClick={() => removeLine(index)} disabled={quote.lineItems.length === 1} className='inline-flex items-center gap-1.5 text-sm font-medium text-red-700 disabled:hidden md:hidden'><Trash2 className='h-4 w-4' /> Remove</button></div></div></div>)}</div></section>
    </div><aside aria-label='Quote summary' className='border-t border-[#d9e0dc] bg-[#f6f8f7] px-5 py-6 lg:border-l lg:border-t-0 lg:px-6'><div className='lg:sticky lg:top-0'><h3 className='text-base font-semibold text-[#17211d]'>Updated quote summary</h3><p className='mt-1 text-sm text-[#65736c]'>Review the new total before saving.</p><dl className='mt-5 space-y-3 border-y border-[#d9e0dc] py-4 text-sm'><SummaryMeta icon={Building2} label='Customer' value={getCustomerName(customer)} /><SummaryMeta icon={MapPin} label='Service address' value={property?.address ?? 'Not selected'} /><SummaryMeta icon={User} label='Assigned to' value={assigneeName || 'Unassigned'} /></dl><div className='mt-5 space-y-4'><div className='flex items-center justify-between text-sm'><span className='text-[#65736c]'>Subtotal</span><strong className='font-semibold tabular-nums text-[#17211d]'>{formatCurrency(totals.subtotal)}</strong></div><div><div className='mb-1.5 flex items-center justify-between'><span id='edit-quote-discount-label' className='text-sm font-medium text-[#536159]'>Discount</span><span className='text-xs tabular-nums text-[#718078]'>-{formatCurrency(totals.discountAmount)}</span></div><div className='flex overflow-hidden rounded-md border border-[#cbd5d0] bg-white'><button type='button' onClick={() => updateField('discountType', quote.discountType === DiscountType.Percentage ? DiscountType.FixedAmount : DiscountType.Percentage)} aria-label='Toggle discount type' className='flex h-10 w-10 shrink-0 items-center justify-center border-r border-[#d9e0dc] bg-[#f4f7f5] text-[#456157]'><>{quote.discountType === DiscountType.Percentage ? <Percent className='h-4 w-4' /> : <CircleDollarSign className='h-4 w-4' />}</></button><input id='edit-quote-discount' aria-labelledby='edit-quote-discount-label' type='number' min='0' step='0.01' value={quote.discountValue} onChange={(event) => updateField('discountValue', Number(event.target.value) || 0)} className='h-10 min-w-0 flex-1 px-3 text-sm tabular-nums outline-none' /></div></div><div><div className='mb-1.5 flex items-center justify-between'><span id='edit-quote-tax-label' className='text-sm font-medium text-[#536159]'>Tax rate</span><span className='text-xs tabular-nums text-[#718078]'>{formatCurrency(totals.taxAmount)}</span></div><div className='relative'><Percent className='pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[#718078]' /><input id='edit-quote-tax' aria-labelledby='edit-quote-tax-label' type='number' min='0' step='0.01' value={quote.taxRate} onChange={(event) => updateField('taxRate', Number(event.target.value) || 0)} className={`${fieldClass} pr-9 tabular-nums`} /></div><p className='mt-1.5 text-xs text-[#718078]'>Applied to taxable items after discount.</p></div></div><div className='mt-6 border-t-2 border-[#8fa198] pt-4'><div className='flex items-end justify-between gap-4'><span className='font-semibold text-[#24332c]'>New total</span><strong className='text-2xl font-semibold tabular-nums text-[#17211d]'>{formatCurrency(totals.total)}</strong></div>{totals.taxableSubtotal > 0 && <p className='mt-1 text-right text-xs text-[#718078]'>{formatCurrency(totals.taxableSubtotal)} taxable</p>}</div><div className='mt-6 flex items-start gap-2 rounded-md bg-[#e8f1ed] p-3 text-xs leading-5 text-[#28513f]'><Check className='mt-0.5 h-4 w-4 shrink-0' /><span>Changes are saved to this quote and reflected on its detail page.</span></div></div></aside></div></div><footer className='shrink-0 border-t border-[#d9e0dc] bg-white px-5 py-3 sm:px-7'>{submitError && <div role='alert' className='mb-3 flex items-start gap-2 rounded-md bg-red-50 px-3 py-2 text-sm text-red-800'><AlertCircle className='mt-0.5 h-4 w-4 shrink-0' /><span>{submitError}</span></div>}<div className='flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between'><p className='text-xs text-[#718078]'><span className='text-red-700'>*</span> Required fields</p><div className='flex gap-2'><button type='button' onClick={handleClose} disabled={isSubmitting} className='h-10 flex-1 rounded-md border border-[#cbd5d0] bg-white px-4 text-sm font-semibold text-[#34443c] hover:bg-[#f2f5f3] disabled:opacity-50 sm:flex-none'>Cancel</button><button type='submit' disabled={isSubmitting} className='inline-flex h-10 flex-1 items-center justify-center gap-2 rounded-md bg-[#0d5944] px-4 text-sm font-semibold text-white shadow-[0_2px_8px_rgba(13,89,68,0.18)] hover:bg-[#084936] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#0d5944] focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-60 sm:flex-none'>{isSubmitting ? <LoaderCircle className='h-4 w-4 animate-spin' /> : <Check className='h-4 w-4' />}{isSubmitting ? 'Saving...' : 'Save changes'}</button></div></div></footer></form>
  </Modal>;
};

const makeLineItem = (isOptional = false): TUpdateLineItem => ({ name: '', unitPrice: 0, quantity: 1, description: '', isOptional, isTaxable: false });
const makeQuote = (source: TQuote): TUpdateQuote => ({ id: source.id, assignedToUserId: source.assignedToUserId || '', lineItems: source.lineItems.map((item) => ({ ...item })), title: source.title, propertyId: source.propertyId || '', discountType: source.discountType, discountValue: source.discountValue, taxRate: source.taxRate * 100, source: source.source || '' });
const calculateTotals = (quote: TUpdateQuote) => { const round = (value: number) => Math.round((value + Number.EPSILON) * 100) / 100; const subtotal = round(quote.lineItems.reduce((sum, item) => sum + item.quantity * item.unitPrice, 0)); const taxableBeforeDiscount = round(quote.lineItems.reduce((sum, item) => sum + (item.isTaxable ? item.quantity * item.unitPrice : 0), 0)); const discountAmount = round(quote.discountType === DiscountType.Percentage ? subtotal * quote.discountValue / 100 : quote.discountValue); const taxableDiscount = subtotal && taxableBeforeDiscount ? round(discountAmount * taxableBeforeDiscount / subtotal) : 0; const taxableSubtotal = round(taxableBeforeDiscount - taxableDiscount); const taxAmount = round(taxableSubtotal * quote.taxRate / 100); return { subtotal, discountAmount, taxableSubtotal, taxAmount, total: round(subtotal - discountAmount + taxAmount) }; };
const Field = ({ label, htmlFor, hint, required, children }: { label: string; htmlFor: string; hint?: string; required?: boolean; children: React.ReactNode }) => <div className='min-w-0'>
  {/* The field control is supplied by the caller; the label still wraps native inputs. */}
  {/* eslint-disable-next-line jsx-a11y/label-has-for */}
  <label id={`${htmlFor}-label`} htmlFor={htmlFor} className='block text-sm font-semibold text-[#33423a]'><span className='mb-1.5 flex items-center justify-between gap-3'><span>{label}{required && <span className='ml-1 text-red-700'>*</span>}</span>{hint && <span className='text-xs font-normal text-[#718078]'>{hint}</span>}</span><span className='block'>{children}</span></label>
</div>;
const DisplayField = ({ label, children }: { label: string; children: React.ReactNode }) => <div className='min-w-0'><span className='mb-1.5 block text-sm font-semibold text-[#33423a]'>{label}</span>{children}</div>;
const SectionHeading = ({ id, title, description }: { id: string; title: string; description: string }) => <div><h3 id={id} className='text-base font-semibold text-[#17211d]'>{title}</h3><p className='mt-1 text-sm leading-6 text-[#65736c]'>{description}</p></div>;
const SummaryMeta = ({ icon: Icon, label, value }: { icon: typeof Building2; label: string; value: string }) => <div className='grid grid-cols-[18px_88px_minmax(0,1fr)] items-start gap-2'><Icon className='mt-0.5 h-4 w-4 text-[#456157]' /><dt className='text-[#718078]'>{label}</dt><dd className='truncate text-right font-medium text-[#33423a]' title={value}>{value}</dd></div>;
const getCustomerName = (customer?: TQuote['customer']) => customer ? (customer.isCompany && customer.companyName ? customer.companyName : customer.fullName || `${customer.firstName ?? ''} ${customer.lastName ?? ''}`.trim() || 'Unnamed customer') : 'Customer unavailable';
const getInitials = (name: string) => name.split(' ').filter(Boolean).slice(0, 2).map((part) => part[0]).join('').toUpperCase();
export default EditQuoteModal;
